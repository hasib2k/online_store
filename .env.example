# Example environment variables — copy to `.env` or `.env.local` on the server (DO NOT COMMIT)
# Use a secure random string for admin password
ADMIN_PASSWORD=your-strong-password

# Production database URL (Postgres recommended)
# PostgreSQL example:
# DATABASE_URL=postgresql://DB_USER:DB_PASS@DB_HOST:5432/DB_NAME?schema=public
# MySQL example:
# DATABASE_URL=mysql://DB_USER:DB_PASS@DB_HOST:3306/DB_NAME
DATABASE_URL=

# App port (optional)
PORT=3000

# Public site URL (used for absolute links)
NEXT_PUBLIC_BASE_URL=https://your-domain.example

# NextAuth / session secret (use a long random string)
NEXTAUTH_SECRET=some-long-random-string

# Node environment
NODE_ENV=production
